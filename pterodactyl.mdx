---
title: "Fix: Pterodactyl"
---

In questa pagina troverai lo script per risolvere il problema di mancata connettività all'interno dei container docker di Pterodactyl.

<Warning>
  Prima di procedere assicurati di aver segutito la [guida ufficiale di installazione Wings](https://pterodactyl.io/wings/1.0/installing.html)

  e di aver spento tutti i container (server) sul tuo pannello Pterodactyl
</Warning>

1. Installa cURL sul tuo server

   ```bash
   apt install curl
   ```
2. Esegui lo script automatico

   ```bash
   curl -sSL https://get.docker.com/ | CHANNEL=stable bash
   ```

   Questo script corregge l'mtu nella configurazione Wings (`/etc/perodactyl/config.yml`) e riavvia in automatico i servizi docker e wings.
3. Avvia il tuo server e verifica il corretto funzionamento

   Se riscontri ancora problemi non esitare a contattarci